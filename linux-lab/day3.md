Course sections finished: Reading Files, Creating/Moving Files, Wildcards & Replacements, Output Streams, Input Streams, Pipes, Least Power, Superuser, Group Permissions, Permission Shortcuts

Commands covered: less, man, cat, head, tail -f, mkdir -p, touch, rm -r/-f, cp -R, mv, tar -cf/-zcf/-xzf -C, wildcards * ? [], expansions {}, streams stdin/stdout/stderr, redirection > >> 1> 2> &>, /dev/null, pipes |, whoami, /etc/passwd, sudo, su, usermod -aG, permissions ls -l, chown user:group, chmod u=rw,g=,o=, numeric perms, chmod +x


Key takeaways:

- less + /search + n/N
-stdout vs stderr and redirecting both
-wildcards and brace expansion happen in the shell, not the command
-least-privilege mindset around sudo / root
-chmod symbolic is safer than defaulting to 777
What is a flag?
    - flags are arguments passed to commands to change how they behave.
    -many programs allow to be lazy and combine flags together ex. ls -la which is same as ls -l -a (order does not matter either) (this depends on the individual command we are running) (double dash long form one dash short hand form)
    -we can pass parameters to flags too ex. --ignore=Name
#### JavaScript recap — DSA essentials

* **Variables:** `let` vs `const` (when I use each)
* **Equality:** always use `===` (why)
* **Loops I’ll use in DSA:** `for`, `for...of`
* **Data structures:**

  * `Map` (freq/count)
  * `Set` (dedupe/seen)
* **Arrays:**

  * mutate: `push`, `pop`, `shift`, `unshift`, `splice`
  * non-mutate: `slice`
* **Strings:** `split`, `join`
* **Functions:** arrow vs function (1 line)
* **Scope:** block scope (`let/const`) basic rule

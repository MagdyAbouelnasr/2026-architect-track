What is execution context?
- created to run the code of a function 
    -Thread of execution (go through the code line by line and run(execute) each line)
    -Memory

What is the call stack?
    -what function is currently being run and where will it go back to?
